export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: 24 }}>
      <h1>Todo API</h1>
      <p>JSON API. See <code>/api/v1/*</code> endpoints.</p>
    </main>
  );
}
